# =============================================================================
# Pump Station Control System - Environment Configuration
# =============================================================================
# Copy this file to .env and customize as needed: cp .env.example .env

# =============================================================================
# Modbus Configuration
# =============================================================================
MODBUS_IO_HOST=modbus-sim       # Docker service name for Modbus simulator
MODBUS_IO_PORT=5020             # Modbus TCP port

# =============================================================================
# MQTT Broker Configuration
# =============================================================================
MQTT_HOST=hivemq                # Docker service name for HiveMQ broker
MQTT_PORT=1883                  # MQTT protocol port (standard)
HIVEMQ_CONTROL_CENTER_PORT=8081 # HiveMQ web UI port

# =============================================================================
# API and UI Ports
# =============================================================================
GRAPHQL_PORT=4123               # Tentacle GraphQL API port
TENTACLE_UI_PORT=3000           # Tentacle monitoring web UI port
SIMULATOR_UI_PORT=8080          # Modbus simulator web UI port

# =============================================================================
# Modbus Register Mapping
# =============================================================================
# Sensor registers: 2000-2003 (holding registers)
# Control coils: 2500-2502 (coils)
LOCAL_STOP_REGISTER=2500        # Normally closed stop button (true = not pressed)
LOCAL_START_REGISTER=2501       # Normally open start button (true = pressed)
PUMP_STATE_COIL=2502            # Pump state output (read by simulator)

# =============================================================================
# Task Scan Rates (milliseconds)
# =============================================================================
TASK_SENSOR_MONITOR_RATE_MS=100 # Sensor monitoring task (fast)
TASK_PUMP_CONTROL_RATE_MS=500   # Pump control logic task (slower)

# =============================================================================
# Container Configuration
# =============================================================================
TENTACLE_UI_VERSION=v0.0.7-amd64      # Tentacle UI version
DENO_IMAGE=joyja/deno-dev:latest # Deno runtime image
